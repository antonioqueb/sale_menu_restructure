<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Desactivar menú "Orders" original -->
    <record id="sale.sale_order_menu" model="ir.ui.menu">
        <field name="active" eval="False"/>
    </record>

    <!-- Desactivar también el menú NATIVO "Quotations": su acción mezcla
         cotizaciones y órdenes, y al re-entrar a la app el webclient cae al
         primer submenú por secuencia — era este, causando que la regla
         'siempre abrir en Sales Orders' se rompiera con pestañas abiertas. -->
    <record id="sale.menu_sale_quotations" model="ir.ui.menu">
        <field name="active" eval="False"/>
    </record>

    <!-- Acción: Quotes (solo draft y sent) -->
    <record id="action_quotations_only" model="ir.actions.act_window">
        <field name="name">Quotes</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">list,kanban,form,calendar,pivot,graph,activity</field>
        <field name="domain">[('state', 'in', ('draft', 'sent'))]</field>
        <field name="context">{'default_state': 'draft'}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new quotation, the first step of a new sale!
            </p>
        </field>
    </record>

    <!-- Acción: Sales Orders (solo sale y done) -->
    <record id="action_sales_orders_only" model="ir.actions.act_window">
        <field name="name">Sales Orders</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">list,kanban,form,calendar,pivot,graph,activity</field>
        <field name="domain">[('state', 'in', ('sale', 'done'))]</field>
        <field name="context">{'create_confirmed_order': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No sales orders yet.
            </p>
        </field>
    </record>

    <!-- Quotes (1) → Holds (3 otro módulo) → Sales Orders (10) -->
    <menuitem id="menu_quotes_root"
              name="Quotes"
              parent="sale.sale_menu_root"
              action="action_quotations_only"
              sequence="1"/>

    <menuitem id="menu_sales_orders_root"
              name="Sales Orders"
              parent="sale.sale_menu_root"
              action="action_sales_orders_only"
              sequence="10"/>

    <!-- Al abrir la app de Ventas se muestra Sales Orders por defecto
         (acción directa en el menú raíz), sin alterar el orden de los
         submenús en la barra. -->
    <record id="sale.sale_menu_root" model="ir.ui.menu">
        <field name="action" ref="action_sales_orders_only"/>
    </record>

    <!-- ============================================================= -->
    <!-- Apps raíz independientes: Quotes y Holds con ícono propio.     -->
    <!-- Acceso de un clic desde el lanzador de aplicaciones, sin pasar -->
    <!-- por los submenús de Ventas (que se conservan intactos).        -->
    <!-- ============================================================= -->

    <menuitem id="menu_quotes_app"
              name="Quotes"
              action="action_quotations_only"
              web_icon="sale_menu_restructure,static/description/icon_quotes.svg"
              sequence="6"/>

    <menuitem id="menu_holds_app"
              name="Holds"
              action="stock_lot_dimensions.action_stock_lot_hold_order"
              web_icon="sale_menu_restructure,static/description/icon_holds.svg"
              sequence="7"/>

</odoo>